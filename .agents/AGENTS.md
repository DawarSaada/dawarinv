# GitHub Push Rule
Unless explicitly instructed by the user, DO NOT run `git push` or push updates to the remote GitHub repository. You may use `git add` and `git commit` to save progress locally, but wait for the user to say "push" before running `git push`.
